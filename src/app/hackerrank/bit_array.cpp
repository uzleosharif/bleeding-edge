

#include <iostream>

// spec:
// ****
// you are given 4 integers: N,S,P,Q
// you will use them in order to create the sequence a with the following pseudo-code
// a[0] = S (module 2^31)
// for i = 1 to N-1
//   a[i] = a[i-1]*P + Q (module 2^31)
// your task is to calculate the number of distinct integers in the sequence a

// input-format: 4 space separated integers on a single line, N, S, P, Q respectively
// output-format: a single integer that denotes the number of distinct integers in the sequence a
// constraints:
// 1 <= N <= 10^8
// 0 <= S,P,Q < 2^31

// UI: read input from STDIN, print output to stdout

auto main() -> int {
  // read/parse input from STDIN

  // constraint checking for error-handling

  // algo

  // print result to STDOUT
}